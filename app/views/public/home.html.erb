<%= render 'intro' %> <!-- intro -->

<% if @services.count > 0 %>
	<%= render 'service' %> <!-- service -->
<% end %>

<% if @products.count > 0 %>
	<%= render 'product' %>  <!-- product -->
<% end %>
